/** Screen 1 und 2 — Bibliothek (Blaetter `1c`, `1d`). */
import React from 'react';

import { LibraryScreen } from '../../src/screens/library/LibraryScreen';

export default function LibraryRoute() {
  return <LibraryScreen />;
}
