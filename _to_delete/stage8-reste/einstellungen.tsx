/**
 * Screen 14 — Einstellungen (Blatt `3i`), viertes Tab-Ziel.
 */
import React from 'react';

import { SettingsScreen } from '../../src/screens/settings/SettingsScreen';

export default function SettingsRoute() {
  return <SettingsScreen />;
}
